{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.invalid/openscope-params/schemas/launcher/modules/session_creator/1.0.0.json",
  "title": "Module Parameters: session_creator",
  "description": "Creates the session folder structure (and often initializes metadata files) before acquisition begins.",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "force": {"type": "boolean", "description": "If true, overwrite/recreate an existing session folder if present.", "default": false}
  }
}
