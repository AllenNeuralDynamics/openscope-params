{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.invalid/openscope-params/schemas/launcher/modules/experiment_notes_finalize/1.0.0.json",
  "title": "Module Parameters: experiment_notes_finalize",
  "description": "Post-acquisition notes step: optionally preview notes content and prompt the operator to confirm notes are complete.",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "experiment_notes_filename": {"type": "string", "description": "Relative path (inside the session folder) for the notes file.", "examples": ["notes/experiment_notes.txt"]},
    "experiment_notes_encoding": {"type": "string", "description": "Text encoding used when reading the notes file.", "default": "utf-8"},
    "experiment_notes_preview": {"type": "boolean", "description": "If true, print a preview of notes content to the console.", "default": false},
    "experiment_notes_preview_limit": {"type": "integer", "minimum": 0, "description": "Limit for preview output (module-specific).", "default": 2000},
    "experiment_notes_confirm_prompt": {"type": "string", "description": "Prompt shown to the operator to confirm notes are complete.", "examples": ["Confirm experiment notes are saved; press Enter to finish."]}
  }
}
