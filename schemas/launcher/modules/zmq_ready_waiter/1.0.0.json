{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.invalid/openscope-params/schemas/launcher/modules/zmq_ready_waiter/1.0.0.json",
  "title": "Module Parameters: zmq_ready_waiter",
  "description": "Waits for a ZMQ 'ready' message from an upstream component. Used to block until a dependency reports readiness.",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "zmq_ready_waiter_timeout": {"type": "number", "minimum": 0, "description": "Timeout in seconds before failing.", "default": 30}
  }
}
